{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "desc": "Capability for the main window",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "shell:allow-open"
  ]
}
